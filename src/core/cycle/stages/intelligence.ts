/**
 * Never Rest Capital — Cycle Stage: INTELLIGENCE
 *
 * Phase 5b target. Extracts agent-v3.2.ts lines 6176–6357.
 *
 * Responsibilities:
 *   - Volume spike detection
 *   - DEX intelligence aggregation
 *   - On-chain flow recording
 *   - Performance review (win/loss streaks, pattern analysis)
 *   - Threshold adaptation (dynamic confluence / sizing)
 *
 * Outputs on ctx:
 *   - (ctx enriched with regime + intelligence data via stateManager)
 *
 * DO NOT EXTRACT until Phase 5b session.
 */

import type { CycleContext } from '../../types/cycle.js';

export async function intelligenceStage(ctx: CycleContext): Promise<CycleContext> {
  throw new Error('[Phase 5b] intelligenceStage not yet extracted — monolith handles INTELLIGENCE inline (L6176–6357)');
  return ctx;
}
