/**
 * Never Rest Capital — Reporting Module
 * Prompt formatting functions extracted from agent-v3.2.ts (Phase 9 refactor)
 */

export { sf, formatIntelligenceForPrompt, formatIndicatorsForPrompt } from './formatting.js';
