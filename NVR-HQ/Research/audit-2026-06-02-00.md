# NVR Strategy Audit — 2026-06-02T00:00Z

## Run Type
Hourly autonomous agent run (Medic + Scout + Auditor)

## Medic Status
**BLOCKED — API inaccessible**

All Railway production endpoints returned HTTP 403 "Host not in allowlist":
- `GET /api/errors` → 403
- `GET /api/balances` → 403

This container's outbound IP is not on the Railway service's allowlist.
No error patterns could be observed. No circuit breaker state read.
No fix applied. **Cannot confirm bot is healthy or unhealthy.**

Henry: if the bot is showing symptoms (trade failures, Telegram silence, etc.),
the medic script cannot run from this environment. Consider:
1. Whitelisting the Claude Code on the Web container range in Railway network settings
2. Or running `curl /api/errors` from a Railway shell session directly

---

## Scout Status
**Ran (17 days since last scout) — 1 unverified candidate identified**

Last real scout commit: 2026-05-16 (VEIL/OPENX, both reverted per Rule 1).

### Network constraints
All DEX data APIs (GeckoTerminal, DexScreener, DefiLlama) returned 403 from this container.
Scout relied on web search only — liquidity/volume/pool-age data **unverified** from primary sources.

### Candidates evaluated

| Token | Not in Registry | Volume Source | Liquidity (Base) | Pool Age | Score | Decision |
|-------|----------------|---------------|------------------|----------|-------|----------|
| cbMEGA | ✓ | CMC: $2.19M/24h | UNKNOWN (API blocked) | UNKNOWN | N/A — unverifiable | PROPOSAL filed |
| DEGENINME | ✓ | Trending on DexScreener (general mention) | UNKNOWN | UNKNOWN | N/A | INSUFFICIENT DATA |

### Action
- TOKEN_REGISTRY unchanged (Rule 1: cohort locked until ~2026-06-15)
- `COHORT_PROPOSAL_2026-06-02.md` written to repo root for Henry's review
- cbMEGA is the one candidate worth Henry verifying manually on BaseScan + Aerodrome

---

## Auditor Status
**BLOCKED — API inaccessible**

All live data endpoints returned 403:
- `GET /api/trades?limit=50&include_failures=true` → 403
- `GET /api/portfolio` → 403
- `GET /api/patterns` → 403
- `GET /api/adaptive` → 403

Win rate, drawdown, losing streak, and market regime cannot be computed.
Audit trigger conditions could not be evaluated. **No audit performed.**

### Research pre-loaded (for context — not acted on without trigger data)

**Signal quality (from web search):**
- Mirroring confirmed Smart Money wallets on-chain achieves ~65% win rate vs 41% standalone (Nansen-style Smart Money labels)
- Multi-indicator confluence with no single veto is validated approach in 2026 bot literature
- NVR already implements SWARM agent voting (flow 35%, momentum 20%, risk 25%) — aligned

**Execution efficiency:**
- Aerodrome → Aero merger (Q2 2026) embeds MEV auctions into Slipstream V3
  - MEV extraction now benefits LPs/veAERO instead of sandwich bots
  - 1-2% slippage may be sufficient when using the new MEV-protected pools
  - TWAP_ADVERSE_MOVE_PCT (currently 1.0%) and TWAP_SLICE_INTERVAL_MS (12s) may benefit
    from recalibration once Aero V3 launches (expected improvement: lower adverse fill rate)
- No implementable change today (Aero not yet live)

**Position sizing:**
- Quarter-Kelly (0.25) already implemented — matches 2026 research consensus for sustained bear
- Half-Kelly research shows 75% of optimal growth with dramatically lower drawdown
- NVR is already at 0.25 (Quarter-Kelly) — can't go lower without hurting meaningful P&L
- Bear-adjusted KELLY_FRACTION already optimized; no change recommended

**Competitive intelligence:**
- MEV protection via private RPC (sequencer-direct first) already in BASE_RPC_ENDPOINTS
- Slippage tolerance of 1-2% valid for Base L2 in MEV-protected mode (QuickNode research)
- NVR uses TWAP + VWS filter + gas optimization — implementation is already above average

### Finding ratings

| Area | Finding | Impact | Complexity | Priority | Implementable? |
|------|---------|--------|------------|----------|----------------|
| Signal | Smart Money wallet tracking | 4 | 5 | 0.8 | No — too complex, no data source |
| Execution | Aero V3 slippage recalibration | 3 | 2 | 1.5 | Not yet (Aero not launched) |
| Sizing | Quarter-Kelly already optimal | 1 | 1 | N/A | Nothing to do |
| Competitive | Private RPC already in use | 1 | 1 | N/A | Already implemented |

**Top implementable finding:** None meet threshold (priority >= 2.0, risk low/medium, ≤10 lines)
without live trigger data to confirm what's actually underperforming.

---

## Action Taken
- No code changes to `agent-v3.2.ts` or `src/core/config/constants.ts`
- `COHORT_PROPOSAL_2026-06-02.md` committed (scout candidate, Rule 1 compliant)
- This research report committed to NVR-HQ/Research/

## Watch List for Henry

1. **cbMEGA** — verify on BaseScan, check Aerodrome pool. If liquidity qualifies, add to TOKEN_REGISTRY after June 15 window close.

2. **Aerodrome → Aero router migration** — when Aero V3 launches (expected Q2 2026), check if Slipstream router addresses change. If routing errors spike post-launch, update router config in agent-v3.2.ts.

3. **API access from Claude Code on the web** — the autonomous agent cannot run Medic or Auditor while the Railway API is blocked. Consider adding a shared secret header (`X-Medic-Token`) to the `/api/errors` and `/api/balances` endpoints that bypasses the host allowlist, allowing the agent container to authenticate.

Henry: review this branch → merge to main only when ready (these are proposals/reports only — no trading logic changed).
