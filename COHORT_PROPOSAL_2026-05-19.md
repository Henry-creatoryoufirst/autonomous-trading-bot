# Cohort Scout Proposal — 2026-05-19T07:08Z

**Status:** No additions proposed. See human action items below.

---

## Context

This proposal was generated by the hourly agent run (Scout job). Last scout commit was
2026-05-16 10:28 EDT — 72+ hours ago, triggering a fresh scan.

**Important override in effect:** Per CLAUDE.md Rule 1 (Option B window active through
~2026-06-15), TOKEN_REGISTRY modifications are frozen. All scout candidates go here
rather than into `token-registry.ts`.

---

## Scan Limitations

**GeckoTerminal API:** Blocked by remote execution environment network policy
(`Host not in allowlist`). Could not retrieve:
- `GET /api/v2/networks/base/trending_pools`
- `GET /api/v2/networks/base/new_pools`
- Per-token liquidity/volume data

**Quality filter** (liquidity > $100K, 24h vol > $50K, pool age > 3 days) could NOT be
applied against live data. All assessments below are based on web search aggregates only
and should be treated as **unverified**.

---

## Candidates Evaluated

| Token | Symbol | Notes | Verdict |
|-------|--------|-------|---------|
| Osaka Protocol | OSAK | Multi-chain meme; ~$26M MCap May-2026; ~$56K global 24h vol. Primarily Ethereum native, Base pool size unknown. | ❌ FAIL — volume marginal globally, Base-specific pool likely well below $50K threshold |
| Aerodrome Unified | AER | Q2 2026 MetaDEX upgrade merging Aerodrome + Velodrome; AERO holders receive 94.5% of new supply. Not yet launched as of this scan. | 🔭 MONITOR — no live pool yet, evaluate on launch day |
| Base Native Token | BASE | Brian Armstrong signalled exploration of native Base token; no confirmed launch date or pool. | 🔭 MONITOR — if/when launched, evaluate immediately |

**Tokens already in registry that remain valid:** AERO, VIRTUAL, WETH, cbBTC — core
exposure is covered.

---

## Human Action Items

1. **Manual GeckoTerminal scan** — Visit [geckoterminal.com/base/pools](https://www.geckoterminal.com/base/pools)
   and apply quality filter: liquidity > $100K, 24h vol > $50K, pool age > 3 days,
   not already in `src/core/config/token-registry.ts`. Any qualifiers → add here as
   a follow-up proposal after the Option B window closes (~2026-06-15).

2. **Monitor Aerodrome AER launch** — Once the Aerodrome/Velodrome unified MetaDEX
   token (tentative symbol "AER") goes live in Q2 2026, evaluate for cohort addition.
   AERO is already in registry; the new token may have distinct liquidity.

3. **MOLT status** — MOLT (0xb695559b...) is still present in `TOKEN_REGISTRY`
   (scout 2026-05-14 add). CLAUDE.md states it "had to be reverted." Verify whether
   the revert landed on main or is still outstanding.

---

## No Registry Changes

No `src/core/config/token-registry.ts` modifications were made in this run.
This file is the only permitted output during the Option B benchmark window.
