# Cohort Proposal — 2026-06-29

**Generated by:** Scout routine (claude/cool-sagan-4rf8it)
**Per CLAUDE.md Rule 1:** Tokens are NOT auto-added. This proposal requires human PR review.

---

## Blocked Data Sources This Run

The following sources were **inaccessible** due to egress policy restrictions on the agent proxy:

| Source | Status | Impact |
|--------|--------|--------|
| `autonomous-trading-bot-production.up.railway.app:443` | 403 CONNECT denied | Medic & Auditor blind |
| `api.geckoterminal.com:443` | 403 CONNECT denied | No pool-level metrics available |
| `lunarcrush.ai:443` | 403 CONNECT denied | No social signal data |

**Implication:** The Medic could not check error rates or bot health. The Auditor could not verify win rate / drawdown triggers. This is a recurring risk if the egress policy remains in place.

**Recommended fix:** Ask the session administrator to whitelist `autonomous-trading-bot-production.up.railway.app` and `api.geckoterminal.com` in the agent proxy egress policy, or expose a proxy/mirror endpoint that is reachable.

---

## Candidate Token — AERO v2 / "Aero" Merger Token

**Source:** Web search (dlnews.com, June 2026)

**Background:** Dromos Labs (the team behind Aerodrome Finance on Base and Velodrome on Optimism) has announced a merger of the two protocols into a single unified DEX called **Aero**, targeting launch in early July 2026. Existing AERO holders receive 94.5% of the new token supply.

**Why it's interesting for NVR:**
- Aerodrome is already our primary router (50%+ of Base volume)
- A merged Aerodrome + Velodrome creates a cross-chain DEX with Ethereum mainnet ambitions
- Token conversion mechanics could create arbitrage / migration trading opportunities
- We already hold AERO, so the conversion ratio matters for current positions

**Current data (from web search snippets, not verified on-chain):**
- AERO/WETH pool on Aerodrome: ~$9.83M liquidity, ~$110K 24h volume ✓ passes thresholds
- Aerodrome overall: $12.4M 24h volume, 73.8K transactions ✓

**What we cannot verify right now (GeckoTerminal blocked):**
- New "Aero" token contract address on Base
- Pool liquidity and volume for the new token (doesn't exist yet — pre-launch)
- Pool age (N/A — not yet live)

**Disposition:** HOLD — monitor for new contract address post-launch (~July 2026). Once the new Aero token is live on Base with 3+ days of pool history and verified $100K liquidity / $50K 24h volume, re-run the scout quality filter.

---

## Other Candidates Evaluated

Web searches for "Base L2 new token launches June 2026 high volume" returned only general L2 category articles and promotional content — no specific new tokens with Base addresses, verified pool metrics, and >3 day pool age could be confirmed via search alone without GeckoTerminal API access.

**Result: No tokens pass quality filter this scan — standards maintained.**

---

## Action for Henry

1. Review the egress policy block — if Claude Code sessions can't reach the bot API or GeckoTerminal, the routine is flying blind on every run.
2. Monitor the Dromos Labs "Aero" merger token launch (~July 2026). When the new contract address is published, run a manual scout check against the quality filter before adding to cohort via PR.
3. AERO existing position: confirm conversion ratio when the merger token launches to avoid orphaned positions.
