# Cohort Proposal — 2026-06-12

**STATUS: PROPOSAL ONLY — Do not merge during Option B window (~2026-06-15)**

Generated by: NVR Scout (Run #35)
Option B window closes: ~2026-06-15
Cohort currently locked at: COHORT_QUALITY_7 in `src/core/config/token-registry.ts`

---

## Data Access Limitation This Run

`api.geckoterminal.com` is blocked by the execution environment's network egress
policy (same persistent block as the bot production API). The full quality filter
(liquidity > $100K, 24h vol > $50K, pool age > 3 days) could not be run
programmatically. Findings below are based on WebSearch results only.

Henry: To run the full scout filter manually, visit:
- https://api.geckoterminal.com/api/v2/networks/base/trending_pools?page=1
- https://api.geckoterminal.com/api/v2/networks/base/new_pools?page=1
- https://dexscreener.com/base (sort by 24h volume, filter Base)

---

## Existing TOKEN_REGISTRY Coverage (as of 2026-06-12)

The registry already covers 50+ tokens across all sectors. Full list in
`src/core/config/token-registry.ts`. Notable gaps vs. active Base ecosystem:

| Gap Area | Notes |
|----------|-------|
| Uniswap V4 on Base | UNI not in Base registry (only Ethereum registry) |
| Compound V3 on Base | No COMP or cTokens in Base registry |
| Base ecosystem's $BASE token | Possibly launching/distributed May 2026 |
| Farcaster/Warpcast ecosystem | DEGEN present; other Farcaster tokens may have grown |

---

## WebSearch-Derived Candidates (require manual verification before adding)

### Candidate 1: UNI (Uniswap) on Base
- **Rationale**: Uniswap V4 deployed on Base with deep liquidity; integrated with
  Coinbase Wallet swaps. Present in Ethereum registry but not Base registry.
- **Verification needed**: Confirm Base contract address, liquidity depth >$100K,
  24h vol >$50K. Likely already at significant scale given V4 is major Base DEX.
- **Suggested classification**: sector=DEFI, riskLevel=LOW, minTradeUSD=15

### Candidate 2: cbSUI (Coinbase Wrapped SUI) — speculative
- **Rationale**: cbXRP, cbADA, cbSOL, cbLTC, cbDOGE pattern suggests Coinbase may
  have wrapped SUI for Base. Not confirmed.
- **Verification needed**: Check if contract exists on Base; check Aerodrome pool
  existence and liquidity depth.
- **Suggested classification**: sector=BLUE_CHIP, riskLevel=LOW, minTradeUSD=15

### Candidate 3: WELL (Moonwell) — already in registry
- Already present. No action needed.

---

## Recommendation

**Do not act on this proposal before the Option B window closes (~2026-06-15).**

After June 15:
1. Run the GeckoTerminal API calls manually to get fresh trending + new pool data
2. Apply the full quality filter (liq >$100K, vol >$50K, age >3d, not in registry)
3. Score survivors 1-10 on volume consistency, liquidity depth, momentum, category fit
4. Open a human PR for any candidates scoring ≥6

The cohort (COHORT_QUALITY_7) should be reviewed separately from TOKEN_REGISTRY
additions. TOKEN_REGISTRY changes are lower stakes (just adds tradeable tokens);
COHORT_QUALITY_7 changes affect the alpha attribution benchmark and require
explicit strategic sign-off.

---

## Why No Tokens Were Added This Run

1. **Option B window active**: CLAUDE.md Rule 1 prohibits automatic TOKEN_REGISTRY
   edits until ~2026-06-15 to preserve clean alpha attribution for the benchmark.
2. **GeckoTerminal API blocked**: Cannot run the quality filter without verified
   on-chain data (liquidity, volume, pool age).
3. **WebSearch candidates unverified**: UNI and cbSUI are plausible but not
   confirmed to meet the $100K liquidity / $50K volume / 3-day age thresholds.
