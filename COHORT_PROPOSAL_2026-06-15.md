# Cohort Proposal — 2026-06-15

**Scout run:** 2026-06-15 (Option B window end date ~today)
**Branch:** claude/cool-sagan-s4hs0f
**Rule:** COHORT_QUALITY_7 locked per CLAUDE.md Rule 1 — this is a proposal only. No TOKEN_REGISTRY edits made.

---

## Environment Constraints This Run

- Bot API (`/api/errors`, `/api/balances`, `/api/trades`, etc.) returned **403 Forbidden** — `API_AUTH_TOKEN` not set in this scheduled agent's environment. Medic and Auditor health checks could not run.
- GeckoTerminal API (`api.geckoterminal.com`) **blocked** by network egress policy — pool-level liquidity/volume data could not be fetched directly.
- Quality filter thresholds (>$100k liquidity, >$50k 24h volume) were assessed via web research only; on-chain verification is required before any additions.

**Recommended fix:** Add `API_AUTH_TOKEN` as an environment variable in the scheduled agent's Railway/container config, and allowlist `api.geckoterminal.com` in network egress settings.

---

## Candidate Evaluation

| Token | Symbol | Base Address | Age | 24h Vol (est) | Liquidity | Score | Verdict |
|-------|--------|-------------|-----|----------------|-----------|-------|---------|
| Tea Protocol | TEA | Unverified (possibly 0x7eaa67f8d365bbe27d6278fdc2ba24a1aa71c8e5) | ~11 days (TGE Jun 4) | ~$678k (CEX) | Unknown on Base | 5/10 | ⏳ NEEDS VERIFICATION |
| Extra Finance | EXTRA | 0x2dad3a13ef0c6366220f989157009e501e7938f8 | Established | ~$3.3k/24h | $34.8M TVL in protocol | 3/10 | ❌ FAILS volume filter |

### Candidate 1 — TEA (Tea Protocol)
- **What it is:** Open-source software reward protocol. Launched TGE June 4, 2026 via Aerodrome Ignition on Base.
- **Positives:** Aerodrome Ignition launch (deep initial liquidity), >3 days old, ~$678k reported trading volume (CEX aggregate). Protocol incentives ongoing.
- **Concerns:** Base mainnet contract address unverified. CEX volume ≠ Base DEX volume — actual Base pool liquidity unknown. Low USD price ($0.000093) suggests high dilution risk.
- **Category fit:** Possibly DEFI or AI_TOKENS.
- **Action:** Verify Base contract via BaseScan, fetch pool liquidity from Aerodrome or GeckoTerminal before adding. If Base pool has >$100k liquidity and >$50k/24h volume, this is a reasonable DEFI/HIGH candidate.

### Candidate 2 — EXTRA (Extra Finance)
- **What it is:** Leveraged yield farming protocol, primarily on Optimism, with an Aerodrome EXTRA/WETH pool on Base.
- **Reject reason:** 24h volume $3,331 on Base — well below $50k threshold. Protocol TVL is in the protocol's lending vaults, not the token's pool liquidity.

---

## Already-Tracked Tokens Confirmed Active in June 2026

The following tokens from TOKEN_REGISTRY were confirmed still active and relevant from research:
- AERO (Aerodrome) — $12.4B 30-day volume, $453M TVL ✅
- AAVE — GHO stablecoin on Base hit $120M supply ✅
- WETH / cbBTC — Core cohort, benchmark assets ✅
- VIRTUAL — Virtuals Protocol platform active ✅

---

## Recommendation for Henry

Once the Option B window officially closes (if not already today), the following process is recommended:

1. **Verify TEA on Base:** Check BaseScan for TEA contract, fetch Aerodrome pool for TEA/WETH liquidity and 24h volume.
2. **If passes filters:** Open a PR adding TEA to TOKEN_REGISTRY as:
   ```typescript
   TEA: {
     address: "<verified-base-address>",
     symbol: "TEA", name: "Tea Protocol", coingeckoId: "tea-protocol",
     sector: "DEFI", riskLevel: "HIGH", minTradeUSD: 10, decimals: 18,
   },
   ```
3. **Fix agent environment:** Add `API_AUTH_TOKEN` to scheduled agent env so Medic/Auditor can run health checks next cycle.
4. **Allowlist GeckoTerminal:** Add `api.geckoterminal.com` to egress policy so Scout can fetch pool data directly.

---

*Generated by scheduled scout agent — no TOKEN_REGISTRY changes made per CLAUDE.md Rule 1.*
