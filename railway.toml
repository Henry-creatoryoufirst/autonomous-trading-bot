[deploy]
# This is a background worker service, not a web server
# Disable healthcheck since there's no HTTP endpoint
healthcheckPath = ""
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
