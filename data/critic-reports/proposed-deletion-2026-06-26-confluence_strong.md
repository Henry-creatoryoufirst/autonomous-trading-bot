---
tags: [nvr, critic, proposed-deletion, auto-generated]
date: 2026-06-26
source-report: data/critic-reports/2026-04-24.md
pattern: confluence_strong
status: PROPOSED
---

# Proposed Deletion: `confluence_strong`

Generated by the NVR CRITIC-Driven Deletion Proposer routine on 2026-06-26T14:00:00Z.

> **Re-proposal notice:** A prior proposal was generated on 2026-06-05 (branch `auto/critic-deletion-2026-06-05-confluence_strong`). That branch has been open 21 days without action. The source data is unchanged — no newer CRITIC report has been generated since 2026-04-24. This file is a fresh signal for the same evidence; Henry may prefer to work from the June 5 branch instead.

## Evidence (from data/critic-reports/2026-04-24.md)

| Metric | Value | Threshold | Pass? |
|---|---:|---:|:-:|
| Sample size (n) | 23 | ≥ 20 | ✓ |
| Avg $ per trade | -$3.08 | < $0 | ✓ |
| Win rate | 0% | < 30% | ✓ |

Latest report window: last 168h (274 trades analyzed) · report date 2026-04-24

> ⚠️ **Report age notice:** The source report is now 63 days old. The CRITIC has not generated a newer report since. Pattern behavior may have shifted after v21.27–v21.29. A fresh CRITIC run is recommended before acting.

## What this pattern actually is

`confluence_strong` is **not a named code function** in the bot. It is a CRITIC classifier label applied retroactively to exits whose reasoning text matches:

```
/STRONG_BUY|STRONG_SELL|high.?conviction|confluence/i
```

Found only in `scripts/critic.ts:137`:

```
{ label: 'confluence_strong',    regex: /STRONG_BUY|STRONG_SELL|high.?conviction|confluence/i },
```

In practice this catches two exit sub-types:
1. **Capital liberation sells** — bot sells position A because it mentions `STRONG_BUY` or `high-conviction` for position B in the exit reason (e.g., "Creating buying power for RSR STRONG_BUY signal").
2. **Classifier bleed** — exits using the word "confluence" generically even when the exit is a dry-powder or stale-exit action.

The 0% win rate (0W / 5L / 18BE, 23 trades, avg -$3.08) therefore indicts **liberation-sell behavior**, not a single isolatable code path.

## Suggested code locations (for the human reviewer)

```bash
grep -rn "confluence_strong" agent-v3.2.ts src/ | head -20
```

**Matches:** only the CRITIC classifier definition itself:
```
scripts/critic.ts:137:  { label: 'confluence_strong', regex: /STRONG_BUY|STRONG_SELL|high.?conviction|confluence/i },
```

The reviewer can decide whether to:
- **Reclassify in CRITIC** — narrow the `confluence_strong` regex so liberation-sells are attributed to a separate `liberation_sell` bucket; confirm whether each sub-bucket independently fails the deletion test.
- **Add a liberation-sell gate** — require a minimum gain or hold-time before a position can be sold to fund a new entry. This would suppress the losing sub-class without touching the confluence algorithm.
- **Delete the liberation path** (per v21.27 precedent for `liberateCapital`) — if investigation confirms the liberation-sell behavior is the culprit and it has no countervailing wins, delete the code that triggers "sell A to fund B" exits.

## Decision framework (Henry's `feedback_data_driven_delete` memory)

> When data identifies a money-losing category (n≥20), delete > tune.
> Step 2 of Elon's algorithm beats parameter polishing.

The key question is whether the losing behaviour is in the CRITIC *label* (which captures noise) or the actual *code path* (liberation sells). The June 5 proposal laid this out; the recommendation remains: **investigate CRITIC classification accuracy first**, then delete the confirmed losing code path.

## What to do with this proposal

1. Check whether a fresh CRITIC run changes the `confluence_strong` numbers — if the pattern has healed since v21.29, no action needed.
2. If data holds, narrow the CRITIC regex to separate liberation-sells from true confluence exits, then re-run.
3. If the liberation-sell subclass fails deletion criteria independently, open a PR from this branch (or the June 5 branch) to `staging` targeting the relevant code.
4. Soak on staging per `feedback_staging_deploy`. Promote to `main` after clean soak.

This proposal does NOT modify any code itself. It is a proposal artifact. Code changes happen in the human-reviewed PR.
