---
tags: [nvr, critic, proposed-deletion, auto-generated]
date: 2026-06-05
source-report: data/critic-reports/2026-04-24.md
pattern: confluence_strong
status: PROPOSED
---

# Proposed Deletion: `confluence_strong`

Generated by the NVR CRITIC-Driven Deletion Proposer routine on 2026-06-05T14:00:00Z.

## Evidence (from data/critic-reports/2026-04-24.md)

| Metric | Value | Threshold | Pass? |
|---|---:|---:|:-:|
| Sample size (n) | 23 | ≥ 20 | ✓ |
| Avg $ per trade | -$3.08 | < $0 | ✓ |
| Win rate | 0% | < 30% | ✓ |

Latest report window: last 168h (274 trades analyzed) · report date 2026-04-24

> ⚠️ **Report age notice:** The source report is 42 days old (2026-04-24). The CRITIC has not generated a newer report since. This proposal is based on the most recent available data, but the pattern's behavior may have changed since v21.27–v21.29 were merged. Henry should confirm whether a fresher CRITIC run is possible before acting.

## What this pattern actually is

`confluence_strong` is **not a named code function** in the bot. It is a CRITIC classifier label applied retroactively to exits whose reasoning text matches:

```
/STRONG_BUY|STRONG_SELL|high.?conviction|confluence/i
```

In practice this catches two exit sub-types:
1. **Capital liberation sells** — bot sells position A because it mentions `STRONG_BUY` or `high-conviction` for position B in the exit reason (e.g., "Creating buying power for RSR STRONG_BUY signal").
2. **Classifier bleed** — exits that use the word "confluence" generically (e.g., "only asset with strong technical confluence") even when the exit itself is a dry-powder or stale-exit action.

The 0% win rate (0W / 5L / 18BE, 23 trades, avg -$3.08) therefore indicts the **liberation-sell behavior** more than a single code path.

## Suggested code locations (for the human reviewer)

```bash
grep -rn "confluence_strong" agent-v3.2.ts src/ | head -20
```

**Matches found:**

```
scripts/critic.ts:137:  { label: 'confluence_strong',    regex: /STRONG_BUY|STRONG_SELL|high.?conviction|confluence/i },
```

There is **no code path named `confluence_strong`** in `agent-v3.2.ts` or `src/`. The label lives only in the CRITIC classifier. The actual trading behavior is driven by the capital-liberation / `ai_discretionary` decision flow that happens to produce exit reasons containing these keywords.

### What to grep for instead

```bash
# Find liberation-sell logic (selling to fund a conviction buy)
grep -n "liberateCapital\|liberation\|freeing.*fund\|high.conviction" agent-v3.2.ts | head -20

# Find where STRONG_BUY appears in sell-side logic
grep -n "STRONG_BUY.*sell\|sell.*STRONG_BUY\|fundHighConviction" agent-v3.2.ts | head -20
```

The reviewer should identify whether:
- A discrete code path generates these "liberation sell" exits, or
- The exits come from the general AI decision flow (`ai_discretionary` path) with no explicit hook

## Example losing trades (from source report)

| Token | Realized | Exit reasoning (truncated) |
|---|---:|---|
| cbXRP | -$46.01 | "cbXRP showing mixed signals - MACD bullish but overall market weak…" |
| CRV | -$14.56 | "Creating buying power for RSR STRONG_BUY signal - CRV showing SELL(-22) vs RSR STRONG_BUY(+67)…" |
| HIGHER | -$3.65 | "Capital liberation: freeing $37 to fund high-conviction entry" |

The CRV → RSR trade is the canonical case: bot sold CRV at a loss to fund an RSR entry. If RSR also failed, both legs are losers and both get categorized under different labels (RSR under `ai_discretionary` or `confluence_strong` on entry). The net cost is hidden across two pattern buckets.

## Decision framework (Henry's `feedback_data_driven_delete` memory)

> When data identifies a money-losing category (n≥20), delete > tune.
> Step 2 of Elon's algorithm beats parameter polishing.

The 0% win rate at n=23 is statistically hard to ignore. However, **the path to deletion is not obvious** because:

1. There is no single function to delete — the behavior is emergent from the AI decision flow.
2. The right intervention may be tightening the liberation-sell criterion (require the target signal score to be ≥ some threshold before selling an existing position at a loss) rather than eliminating it outright.
3. This is also during the **Option B 30-day benchmark window (2026-05-15 – ~2026-06-15)**. Per CLAUDE.md ground rules, strategy-shape changes during the window muddy alpha attribution. Consider deferring code changes until after ~2026-06-15.

The reviewer can decide whether to:
- **Tighten** the liberation-sell trigger: only sell for a STRONG_BUY ≥ +X signal and require the target token NOT to be the same token we've lost on recently
- **Investigate** whether the CRITIC classifier is correctly attributing the loss to the liberation-sell logic vs. noise from the `confluence` keyword matching innocuous exits
- **Defer** to post-Option-B-window, then run a fresh CRITIC report first

## What to do with this proposal

1. Run a fresh CRITIC report (`npx ts-node scripts/critic.ts`) to confirm the pattern still fires at n≥20 with negative avg after v21.27–v21.29.
2. Grep for the liberation-sell code path per the instructions above.
3. If the behavior is confirmed to be a discrete code path and the pattern persists in newer data, open a PR from this branch (`auto/critic-deletion-2026-06-05-confluence_strong`) to `staging`.
4. Soak on staging per `feedback_staging_deploy`. Promote to `main` after clean soak.

This proposal does NOT modify any code itself. It is a proposal artifact. Code changes happen in the human-reviewed PR.
